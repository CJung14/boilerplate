import { createSlice } from "@reduxjs/toolkit";

export const dummyReducerSlice = createSlice({
  name: "dummyReducer",
  initialState: {},
  reducers: {},
});

export default dummyReducerSlice.reducer;
